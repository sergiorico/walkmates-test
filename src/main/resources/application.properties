spring.application.name=WalkMates
server.port=8080

# Repositories are in-memory (see com.walkmates.repository.inmemory), so no datasource is
# configured by default. H2 is on the classpath for the optional persistence stretch task; wire
# in spring-boot-starter-data-jpa and a datasource if you take that on.
